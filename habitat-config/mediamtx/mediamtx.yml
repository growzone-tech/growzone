paths:
  # ls -la /dev/v4l/by-path/

  cam0:
    runOnDemand: ffmpeg -f v4l2 -i /dev/v4l/by-path/platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.2:1.0-video-index0 -c:v libx264 -pix_fmt yuv420p -preset ultrafast -b:v 600k -f rtsp -rtsp_transport tcp rtsp://127.0.0.1:8554/cam0
    runOnDemandRestart: yes

  cam1:
    runOnDemand: ffmpeg -f v4l2 -i /dev/v4l/by-path/platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.4:1.0-video-index0 -c:v libx264 -pix_fmt yuv420p -preset ultrafast -b:v 600k -f rtsp -rtsp_transport tcp rtsp://127.0.0.1:8554/cam1
    runOnDemandRestart: yes

  cam2:
    runOnDemand: ffmpeg -f v4l2 -i /dev/v4l/by-path/platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.1:1.0-video-index0 -c:v libx264 -pix_fmt yuv420p -preset ultrafast -b:v 600k -f rtsp -rtsp_transport tcp rtsp://127.0.0.1:8554/cam2
    runOnDemandRestart: yes
